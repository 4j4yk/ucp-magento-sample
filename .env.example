# Server
PORT=3000
BASE_URL=http://localhost:3000

# Adobe Commerce / Magento 2
# Example: https://mystore.com
MAGENTO_BASE_URL=https://your-magento-domain.example
# Store view code for REST routes; often 'default'
MAGENTO_STORE_CODE=default
# Admin token (Integration token). See README for steps.
MAGENTO_ADMIN_TOKEN=REPLACE_ME

# Optional: if you want "complete" to place an order automatically without escalation,
# set a payment method code that your store supports (e.g., 'checkmo' for Check/Money order).
PAYMENT_METHOD_CODE=checkmo

# Handoff / continue experience
# If not set, gateway will render a minimal continue page on BASE_URL.
MAGENTO_CHECKOUT_URL=https://your-magento-domain.example/checkout
